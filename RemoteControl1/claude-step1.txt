Прочитай @CLAUDE.md, @TZ.txt и результат предыдущего аудита в текущей сессии. /init НЕ запускать.

Теперь можно приступать к правкам, но только маленьким безопасным пакетом.

Главная цель:
Начать перевод проекта Remote Control к единому бело-розовому enterprise SaaS дизайну и одновременно исправить только самые безопасные явные UI/JS несостыковки, которые не требуют изменения БД, миграций и крупной бизнес-логики.

Важно:
Не переписывать проект с нуля.
Не менять архитектуру.
Не менять модели БД.
Не делать миграции.
Не удалять существующий функционал.
Не ломать Razor handlers.
Не менять TaskService без отдельного подтверждения.
Не переписывать все страницы сразу.
Не делать drag & drop.
Не делать новый календарь.
Не делать новые отчёты.
Не делать комментарии, вложения, сообщения, скриншоты и вебку в этом пакете.
Не делать всё ТЗ сразу.
Работать только первым безопасным пакетом.

Источник требований:
1. @CLAUDE.md
2. @TZ.txt
3. предыдущий аудит проекта
4. текущая структура файлов проекта

Главная дизайн-цель:
Проект должен перестать выглядеть как случайная AI-генерация и начать выглядеть как цельный бело-розовый enterprise SaaS продукт.

Светлая тема по умолчанию:
- фон: #fff4f8 или #fff7fa
- карточки: #ffffff
- мягкие подложки: #fff0f5 и #fffafd
- границы: #f0c8d5, #f3d5df, #f4dce4
- основной текст: #3a2f35
- вторичный текст: #725865, #5f4d56, #8c6c78
- главный акцент: #9b3f62
- тёмный акцент: #8f3456 или #7d2948
- success: #2f7a48 на фоне #e8f6ed
- danger: #a83255 на фоне #ffe8ee

Стиль:
blush pink enterprise SaaS, dusty rose, berry accent, plum text.
Не делать детский розовый сайт.
Не делать открытку.
Не делать форум.
Не делать кислотный розовый.
Не делать neon/glow/cyberpunk/glassmorphism/3D.
Не использовать хаотичные градиенты.
Тени минимальные или отсутствуют.
Border 1px.
Скругление 6–10px, максимум 12px.
Отступы 16/20/24px.
Sidebar 250–270px.
Topbar 68–76px.
Интерфейс должен быть деловым, мягким, спокойным и единым.

Тёмная тема:
Подготовить основу тёмной темы в тёмно-сине/пурпурной палитре.
Она должна быть спокойной, деловой и читаемой.
Не делать cyberpunk.
Не делать кислотные цвета.
Если в проекте уже есть переключатель темы — использовать его.
Если переключателя нет, можно только подготовить CSS-переменные и классы, без сложной логики.

Что можно делать в этом пакете:
1. Добавить или привести в порядок CSS-переменные дизайн-системы.
2. Привести базовые цвета к единой палитре.
3. Унифицировать:
   - body/background;
   - layout;
   - sidebar;
   - topbar;
   - cards;
   - buttons;
   - inputs;
   - selects;
   - textarea;
   - badges;
   - tables;
   - toolbar;
   - modals;
   - modal close buttons;
   - tabs;
   - empty states;
   - disabled states.
4. Исправить безопасные JS/CSS подключения, если они явно сломаны:
   - отсутствующий нужный js на странице;
   - дубль js, который ломает let/const;
   - capture.js/timer.js, если есть ложные ошибки;
   - profile.js/users.js, если профиль не получает нужные функции;
   - tracker/tasks/timer, если список задач трекера не работает.
5. Скрыть или отключить кнопки, которые видны не той роли, если это делается безопасно на Razor/JS уровне и не требует изменения БД.
6. Не менять существующие id/name/data-атрибуты, если на них завязан JS.
7. Не удалять старые обработчики без проверки.
8. Если найденная проблема требует большого изменения, не исправлять её сейчас, а вынести в список “следующий пакет”.

Что обязательно сохранить:
- авторизация;
- роли;
- задачи;
- проекты;
- этапы;
- таймер;
- рабочий день;
- ручные заявки времени;
- календарь;
- отчёты;
- профиль;
- настройки;
- существующие формы;
- существующие модалки;
- существующие серверные handlers.

Единая структура экранов, к которой постепенно приводить:
1. Page header: название, краткое описание, главное действие.
2. Toolbar: поиск, фильтры, сортировка, период.
3. KPI/cards, если экран аналитический.
4. Основная зона: таблица, карточки, календарь или трекер.
5. Side panel или модалка для деталей.
6. Состояния: empty, loading/skeleton, error, disabled.

Но в этом пакете НЕ надо переписывать все страницы под эту структуру. Нужно только подготовить общий дизайн-слой и исправить самые безопасные несостыковки.

Приоритет первого пакета:
1. Дизайн-токены и CSS-переменные.
2. Общие стили layout/sidebar/topbar.
3. Общие стили карточек, таблиц, кнопок, форм, бейджей и модалок.
4. Единый вид disabled-состояний.
5. Единый вид крестиков закрытия модалок.
6. Безопасные подключения JS/CSS, если они явно ломают страницы.
7. Минимальная подготовка светлой розовой темы и тёмной сине-пурпурной темы.

Работай осторожно:
Перед изменениями сначала перечисли файлы, которые собираешься менять.
Потом внеси изменения.
После изменений не продолжай бесконечно расширять задачу.

После правок обязательно:
1. Покажи список изменённых файлов.
2. Кратко объясни, что изменено в каждом файле.
3. Покажи краткий diff или summary diff.
4. Запусти:
   dotnet build
5. Если dotnet build упал — остановись, покажи ошибку и не делай новых правок.
6. Если dotnet build прошёл — напиши:
   - что готово;
   - что проверить руками в браузере;
   - какой следующий маленький пакет делать.

Формат результата:
A. Изменённые файлы.
B. Что исправлено.
C. Что НЕ трогал специально.
D. Результат dotnet build.
E. Что проверить руками.
F. Следующий рекомендуемый пакет.