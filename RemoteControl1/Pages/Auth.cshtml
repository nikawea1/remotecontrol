@page
@model RemoteControl1.Pages.AuthModel
@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Вход | Remote Control</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        :root {
            --primary-dark: #1f4f9f;
            --primary: #2f6fd6;
            --primary-light: #4f8ee8;
            --secondary: #edf4ff;
            --success: #16a34a;
            --warning: #d97706;
            --danger: #dc2626;
            --light: #f5f8fd;
            --dark: #111827;
            --gray: #6b7280;
            --border: #dce6f5;
            --radius: 8px;
            --shadow: 0 18px 48px rgba(31,79,159,0.14);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            backdrop-filter: none !important;
            filter: none !important;
        }

        body {
            min-height: 100vh;
            background: linear-gradient(135deg, #eaf2ff 0%, #f7fafe 46%, #dceaff 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
            position: relative;
            overflow: hidden;
        }

            /* ✅ УЗОР НА ФОНЕ (как ты просила) */
            body::before {
                content: '';
                position: absolute;
                inset: 0;
                background: radial-gradient(circle at 18% 18%, rgba(47,111,214,0.10), transparent 34%), radial-gradient(circle at 82% 78%, rgba(79,142,232,0.14), transparent 32%), linear-gradient(90deg, rgba(31,79,159,0.04) 1px, transparent 1px), linear-gradient(rgba(31,79,159,0.04) 1px, transparent 1px);
                background-size: auto, auto, 44px 44px, 44px 44px;
                pointer-events: none;
                z-index: 0;
            }

        .auth-container {
            position: relative;
            z-index: 1;
            display: flex;
            width: 100%;
            max-width: 1000px;
            min-height: 600px;
            background: white;
            border-radius: 16px;
            overflow: hidden;
            box-shadow: var(--shadow);
            animation: fadeIn 0.5s ease-out;
        }

        @@keyframes fadeIn {
            from

        {
            opacity: 0;
            transform: translateY(20px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }

        }

        .auth-left {
            flex: 1.2;
            background: linear-gradient(160deg, #1f4f9f 0%, #2f6fd6 56%, #4f8ee8 100%);
            position: relative;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
        }

            .auth-left::before {
                content: '';
                position: absolute;
                width: 200%;
                height: 200%;
                background: radial-gradient(circle at 70% 30%, rgba(255,255,255,0.12) 0%, transparent 50%), repeating-linear-gradient(-45deg, rgba(255,255,255,0.03) 0px, rgba(255,255,255,0.03) 2px, transparent 2px, transparent 18px);
                transform: rotate(5deg);
                top: -50%;
                left: -50%;
            }

        .left-content {
            position: relative;
            z-index: 2;
            text-align: center;
            color: white;
            padding: 50px;
            max-width: 500px;
        }

            .left-content i {
                font-size: 5rem;
                margin-bottom: 25px;
                background: rgba(255,255,255,0.12);
                padding: 25px;
                border-radius: 50%;
                box-shadow: none;
                border: 1px solid rgba(255,255,255,0.2);
            }

            .left-content h1 {
                font-size: 2.8rem;
                font-weight: 700;
                margin-bottom: 15px;
                letter-spacing: 2px;
                text-shadow: none;
            }

            .left-content p {
                font-size: 1.2rem;
                line-height: 1.6;
                opacity: 0.95;
                margin-bottom: 50px;
                font-weight: 300;
            }

        .features-mini {
                background: rgba(255,255,255,0.12);
                backdrop-filter: none;
                border-radius: 16px;
            padding: 30px;
            border: 1px solid rgba(255,255,255,0.15);
            text-align: left;
        }

        .feature-mini-item {
            display: flex;
            align-items: center;
            gap: 18px;
            margin-bottom: 25px;
        }

            .feature-mini-item:last-child {
                margin-bottom: 0;
            }

            .feature-mini-item i {
                font-size: 2rem;
                padding: 0;
                background: none;
                box-shadow: none;
                border: none;
                margin: 0;
                color: var(--secondary);
                width: 40px;
                text-align: center;
            }

        .feature-mini-text h4 {
            font-size: 1.2rem;
            margin-bottom: 5px;
            color: white;
        }

        .feature-mini-text p {
            font-size: 0.95rem;
            margin-bottom: 0;
            opacity: 0.8;
        }

        .auth-right {
            width: 50%;
            min-width: 420px;
            background: linear-gradient(180deg, #ffffff 0%, #f7fafe 100%);
            border-left: 1px solid rgba(31, 79, 159, 0.10);
            display: flex;
            flex-direction: column;
            justify-content: center;
            padding: 50px 45px;
        }

        .auth-header {
            margin-bottom: 35px;
        }

            .auth-header h2 {
                font-size: 2.4rem;
                color: var(--primary-dark);
                font-weight: 700;
                margin-bottom: 12px;
                letter-spacing: -0.5px;
            }

            .auth-header p {
                color: var(--gray);
                font-size: 1.1rem;
            }

                .auth-header p a {
                    color: var(--primary);
                    text-decoration: none;
                    font-weight: 600;
                    transition: color 0.2s;
                    border-bottom: 1px dashed var(--primary-light);
                }

                    .auth-header p a:hover {
                        color: var(--primary-light);
                        border-bottom: 1px solid var(--primary-light);
                    }

        .auth-form {
            display: flex;
            flex-direction: column;
            gap: 22px;
        }

        .form-group {
            display: flex;
            flex-direction: column;
            gap: 6px;
        }

        .form-label {
            font-weight: 600;
            font-size: 0.95rem;
            color: var(--dark);
            display: flex;
            align-items: center;
            gap: 8px;
        }

            .form-label i {
                color: var(--primary);
                font-size: 1rem;
                width: 20px;
            }

        .input-wrapper {
            position: relative;
            display: flex;
            align-items: center;
        }

            .input-wrapper i.input-icon {
                position: absolute;
                left: 16px;
                color: var(--gray);
                font-size: 1.1rem;
            }

        .form-control {
            width: 100%;
            padding: 16px 20px 16px 50px;
            border: 2px solid var(--border);
            border-radius: 16px;
            font-size: 1rem;
            transition: all 0.3s ease;
                background: #ffffff;
        }

            .form-control:focus {
                outline: none;
                border-color: var(--primary);
                background: white;
                box-shadow: 0 0 0 3px rgba(47, 111, 214, 0.22);
            }

            .form-control::placeholder {
                color: #b0bec5;
                font-weight: 400;
            }

        .password-toggle {
            position: absolute;
            right: 16px;
            background: none;
            border: none;
            color: var(--gray);
            cursor: pointer;
            font-size: 1.2rem;
            transition: color 0.2s;
            padding: 5px;
        }

            .password-toggle:hover {
                color: var(--primary);
            }

        .form-options {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin: 5px 0 10px;
        }

        .remember-me {
            display: flex;
            align-items: center;
            gap: 8px;
        }

            .remember-me input[type="checkbox"] {
                width: 18px;
                height: 18px;
                accent-color: var(--primary);
                cursor: pointer;
            }

            .remember-me label {
                font-size: 0.95rem;
                color: var(--dark);
                cursor: pointer;
            }

        .forgot-password {
            color: var(--primary);
            text-decoration: none;
            font-size: 0.95rem;
            font-weight: 500;
            transition: color 0.2s;
            border-bottom: 1px dashed var(--primary-light);
        }

            .forgot-password:hover {
                color: var(--primary-light);
                border-bottom: 1px solid var(--primary-light);
            }

        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            padding: 16px 24px;
            border: none;
            border-radius: 8px;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .btn-primary {
            background: var(--primary);
            color: white;
            width: 100%;
        }

            .btn-primary:hover {
                background: var(--primary-light);
                transform: none;
                box-shadow: none;
            }

        .error-box {
            margin-top: 0;
            padding: 12px 14px;
            border-radius: 14px;
            background: #fee2e2;
            color: var(--danger);
            font-weight: 600;
        }

        .field-error {
            margin-top: 6px;
            color: var(--danger);
            font-size: 0.92rem;
            font-weight: 600;
        }

        @@media (max-width: 992px) {
            .auth-left {
                display: none;
            }

            .auth-right {
                width: 100%;
                min-width: auto;
            }
        }

        @@media (max-width: 576px) {
            .auth-right {
                padding: 35px 25px;
            }

            .form-options {
                flex-direction: column;
                gap: 15px;
                align-items: flex-start;
            }

            .auth-header h2 {
                font-size: 2rem;
            }
        }
    </style>
</head>

<body>
    <div class="auth-container">

        <div class="auth-left">
            <div class="left-content">
                <i class="fas fa-rocket"></i>
                <h1>REMOTE CONTROL</h1>
                <p>Ваш персональный помощник</p>

                <div class="features-mini">
                    <div class="feature-mini-item">
                        <i class="fas fa-stopwatch"></i>
                        <div class="feature-mini-text">
                            <h4>Умный трекер времени</h4>
                            <p>Автоматический учет рабочих часов с детализацией по задачам</p>
                        </div>
                    </div>

                    <div class="feature-mini-item">
                        <i class="fas fa-chart-pie"></i>
                        <div class="feature-mini-text">
                            <h4>Детальная аналитика</h4>
                            <p>Наглядные отчеты о продуктивности и эффективности</p>
                        </div>
                    </div>

                    <div class="feature-mini-item">
                        <i class="fas fa-list-check"></i>
                        <div class="feature-mini-text">
                            <h4>Управление задачами</h4>
                            <p>Контроль дедлайнов и приоритетов в одном месте</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="auth-right">
            <div class="auth-header">
                <h2>Войти в систему</h2>
                <p>Нет аккаунта? <a href="/Regist">Зарегистрироваться</a></p>
            </div>

            <form class="auth-form" method="post">
                @Html.AntiForgeryToken()

                <div class="form-group">
                    <label class="form-label"><i class="fas fa-user"></i> Логин или Email</label>
                    <div class="input-wrapper">
                        <i class="fas fa-user input-icon"></i>
                        <input type="text" class="form-control" asp-for="Input.Email" placeholder="Email или логин" required>
                    </div>
                    <div class="field-error" asp-validation-for="Input.Email"></div>
                </div>

                <div class="form-group">
                    <label class="form-label"><i class="fas fa-lock"></i> Пароль</label>
                    <div class="input-wrapper">
                        <i class="fas fa-lock input-icon"></i>
                        <input type="password" class="form-control" id="loginPassword" asp-for="Input.Password" placeholder="••••••••" required>

                        <button type="button" class="password-toggle" onclick="togglePassword('loginPassword', this)">
                            <i class="fas fa-lock"></i>
                        </button>
                    </div>
                    <div class="field-error" asp-validation-for="Input.Password"></div>
                </div>

                <div class="form-options">
                    <div class="remember-me">
                        <input type="checkbox" id="remember" checked>
                        <label for="remember">Запомнить меня</label>
                    </div>
                    <a href="#" class="forgot-password" onclick="showNotification('Функция восстановления пароля'); return false;">Забыли пароль?</a>
                </div>

                <button type="submit" class="btn btn-primary">
                    <i class="fas fa-right-to-bracket"></i> Войти
                </button>

                @if (!string.IsNullOrWhiteSpace(Model.Error))
                {
                    <div class="error-box">@Model.Error</div>
                }
            </form>
        </div>
    </div>

    <div id="notification" class="notification hidden">
        <div style="display: flex; align-items: center; gap: 10px;">
            <i class="fas fa-circle-info" style="color: var(--primary); font-size: 20px;"></i>
            <div id="notificationMessage">Уведомление</div>
        </div>
    </div>

    <style>
        .notification {
            position: fixed;
            bottom: 30px;
            right: 30px;
            background: #ffffff;
            border: 1px solid rgba(31, 79, 159, 0.12);
            border-radius: 12px;
            padding: 15px 20px;
            box-shadow: none;
            border-left: 4px solid var(--primary);
            z-index: 10000;
            animation: slideIn 0.3s;
            max-width: 350px;
        }

            .notification.hidden {
                display: none;
            }
        @@keyframes slideIn {
            from

        {
            transform: translateX(100%);
            opacity: 0;
        }

        to {
            transform: translateX(0);
            opacity: 1;
        }

        }
    </style>

    <script>
        function togglePassword(inputId, btn) {
            const input = document.getElementById(inputId);
            const icon = btn.querySelector('i');

            if (input.type === 'password') {
                input.type = 'text';
                icon.classList.remove('fa-lock');
                icon.classList.add('fa-lock-open');
            } else {
                input.type = 'password';
                icon.classList.remove('fa-lock-open');
                icon.classList.add('fa-lock');
            }
        }

        function showNotification(message) {
            const notification = document.getElementById('notification');
            const messageEl = document.getElementById('notificationMessage');

            if (!notification || !messageEl) {
                alert(message);
                return;
            }

            messageEl.textContent = message;
            notification.classList.remove('hidden');

            setTimeout(() => {
                notification.classList.add('hidden');
            }, 2000);
        }
    </script>

    <!-- ВАЖНО: section в Layout=null не отработает, поэтому так подключаем внизу -->
    <partial name="_ValidationScriptsPartial" />
</body>
</html>
